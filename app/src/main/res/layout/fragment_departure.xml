<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.departures.DepartureHomeFragmentFixAndDeleteLater">

    <!-- Departure Board Container -->
    <FrameLayout
        android:id="@+id/departure_board_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/departure_divider"
        app:layout_constraintHeight_percent="0.6" />

    <!-- Divider between departure board and other content -->
    <View
        android:id="@+id/departure_divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#EEEEEE"
        app:layout_constraintTop_toBottomOf="@id/departure_board_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Additional departure-related content area -->
    <TextView
        android:id="@+id/departure_additional_content"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="Departure information goes here"
        android:background="@color/white"
        android:textAlignment="center"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/departure_divider" />
</androidx.constraintlayout.widget.ConstraintLayout>